//! Application modules.
//!
//! Each module is self-contained with its own controller, service,
//! repository (trait + implementation), DTOs, and schemas.
//! New modules are auto-registered here by `aksum generate module`.

pub mod health;
// aksum:module_declarations
